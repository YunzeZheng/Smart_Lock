#include <Keypad.h>
#include "main.h"
// Define the keypad size
const byte ROWS = 4;  // Four rows
const byte COLS = 4;  // Four columns

void setup() {
  Serial.begin(115200);  // Start Serial communication
}

void loop() {

  // Empty loop since we're running the test in setup
}